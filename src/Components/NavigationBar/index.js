import { StyledLink, Header } from "./styled"

export const Nav = () => {
    return(
        <Header>
                <StyledLink to="/">
                    Where in the world?
                </StyledLink>
                <p>dark mode</p>
        </Header>
    )
}
